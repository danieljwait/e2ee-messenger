<Window x:Class="MessengerAppClient.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MessengerAppClient.Views"
        mc:Ignorable="d"
        Title="MessengerApp Client" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>

        <!-- Row 1 -->
        <Button x:Name="Reconnect" Grid.Column="5" Grid.Row="1" Margin="5"
                Content="Reconnect to server"/>

        <ScrollViewer  Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="4" Grid.RowSpan="4"
            VerticalScrollBarVisibility="Visible" Margin="5">
            <StackPanel>
                <TextBlock x:Name="Recieved" Text="{Binding Path=Recieved, Mode=OneWay, StringFormat=Server: {0}}"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Row 2-->
        <Button x:Name="Disconnect" Grid.Column="5" Grid.Row="2" Margin="5"
                Content="Disconnect from server"/>

        <!-- Row 3 -->
        <TextBlock x:Name="ConnectionStatus" Grid.Column="5" Grid.Row="3" Margin="5"
                   Text="{Binding Path=ConnectionStatus, Mode=OneWay}" TextAlignment="Center"/>

        <!-- Row 4 -->
        <TextBox x:Name="MessageToSend" Grid.Column="1" Grid.Row="5" Grid.ColumnSpan="4" Margin="5"
                 Text="{Binding Path=MessageToSend, UpdateSourceTrigger=PropertyChanged}"/>
        
        <Button x:Name="Send" Grid.Column="5" Grid.Row="5" Margin="5"
                Content="Send message"/>

    </Grid>
</Window>
